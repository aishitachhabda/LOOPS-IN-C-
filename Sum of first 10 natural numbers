Sum of first 10 natural numbers

#include <stdio.h>

int main() {

int sum = 0; // Initialize sum to 0

// Loop from 1 to 10, adding each number to the sum

for (int i = 1; i <= 10; i++) {

sum = sum + i; // Add current value of 'i' to sum // sum += i;

}

// After the loop, print the total sum

printf("Sum of first 10 natural numbers is: %d\n", sum);

return 0; // End of the program
